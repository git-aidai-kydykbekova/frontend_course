
import React, { useState } from 'react';

function App() {
    const [data, setData] = useState([]);
    const [search, setSearch] = useState('');

    return (
        <div>
            <h1>Data Filter</h1>
        </div>
    );
}

export default App;
